pre-commit:
  parallel: true
  jobs:
    - name: rubocop
      glob: "{*.rb,Rakefile,Gemfile}"
      run: mise exec -- bundle exec rubocop --force-exclusion {staged_files}

pre-push:
  jobs:
    - name: build
      run: mise exec -- bundle exec jekyll build
